# AuthPilot Server (minimal Nest scaffold)

This is a minimal Nest-like TypeScript scaffold to get started.

Quick start:

```bash
cd server
npm install
npm run build
npm start
```

For development with automatic restarts:

```bash
npm run start:dev
```
